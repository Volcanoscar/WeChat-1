<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tcl.wechat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:name=".WeChatApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <!-- 主界面 -->
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" 
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
				<action android:name="android.intent.action.DELETE"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 用户信息显示界面 -->
        <activity android:name=".activity.PersonalInfoActivity"/>
        <!-- 好友信息显示界面 -->
        <activity android:name=".activity.FriendInfoActivity"/>
        <!-- 聊天界面 -->
        <activity android:name=".activity.ChatActivity"/>
        <!-- 联系人列表界面 -->
        <activity android:name=".activity.FriendListActivity"/>
        <!-- 登录界面 -->
        <activity android:name=".activity.LoginActivity"/>
        <!-- 用户注册界面 -->
        <activity android:name=".activity.RegisterActivity"/>
        
        <!-- appWidget -->
        <receiver 
            android:name="com.tcl.wechat.widget.WeChatWidget"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher">
            <intent-filter >
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data 
                android:name="android.appwidget.provider"
                android:resource="@xml/layout_appwidget" />
        </receiver>
        
        <!-- 启动应用消息接收器 -->
        <receiver 
            android:name="com.tcl.wechat.receiver.StartAppReceiver">
            <intent-filter >
                <action android:name="com.tcl.action.MAINVIEW"/>
                <action android:name="com.tcl.action.USERINFO"/>
                <action android:name="com.tcl.action.PLAYVIEW"/>
                <action android:name="com.tcl.action.CHATVIEW"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
